[user]
    name = Ben Du
    email = longendu@yahoo.com
[core]
    editor = nvim
    pager = delta
    excludesfile = ~/.gitignore
    fileMode = true
    compression = 0
[interactive]
    diffFilter = delta --color-only
[add.interactive]
    useBuiltin = false
[delta]
    navigate = true  # use n and N to move between diff sections
    light = false  # set to true if the terminal has a light background color
    line-numbers = true
[merge]
    tool = vimdiff
    conflictstyle = diff3
[diff]
    colorMoved = default
[color]
    ui = auto
[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
[difftool]
    prompt = false
[mergetool]
    prompt = false
[pull]
    rebase = false

